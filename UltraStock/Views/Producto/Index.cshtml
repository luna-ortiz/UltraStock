@model List<UltraStock.Models.Producto>

<h2 class="mb-3">Lista de Productos</h2>

@if (Context.Session.GetString("Rol") == "admin") //si es admin
{
    <a class="btn btn-success mb-3" href="/Producto/Create">+ Nuevo Producto</a>
}

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Stock</th>
            <th>Categoría</th>
            @if (Context.Session.GetString("Rol") == "admin")//si es admin
            {
                <th>Acciones</th>
            }
            
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nombre</td>
                <td>@item.Precio</td>
                <td>@item.Stock</td>
                <td>@item.Categoria?.Nombre</td>


                @if (Context.Session.GetString("Rol") == "admin") //si es admin
                {
                    <td>
                        <a class="btn btn-warning btn-sm"
                           href="/Producto/Edit/@item.Id">Editar</a>

                        <a class="btn btn-danger btn-sm"
                           href="/Producto/Delete/@item.Id">Eliminar</a>
                    </td>
                }
                
            </tr>
        }
    </tbody>
</table>